"use client";

import { observer } from "mobx-react-lite";

import styles from "./styles/index.module.scss";

const Community = observer(() => {
  return <div className={styles.community}></div>;
});

export default Community;
